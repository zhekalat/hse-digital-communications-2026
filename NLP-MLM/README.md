# 🎯 MLM Game - Игра для обучения Masked Language Modeling

Интерактивная веб-игра для демонстрации процесса обучения языковых моделей через задачу MLM (Masked Language Modeling).

## 📖 Описание

Эта игра помогает студентам понять, как работают современные языковые модели (BERT, RoBERTa и др.). Студенты выступают в роли модели и должны заполнить пропущенные слова в тексте, используя контекст.

## 🎮 Возможности

- **5 текстов на разные темы**: литература, история, наука, природа, культура
- **Интерактивные поля ввода** для заполнения пропусков
- **Система подсказок** (части речи, синонимы)
- **Автоматическая проверка** ответов
- **Статистика** заполнения и правильных ответов
- **Показ правильных ответов** для обучения
- **Адаптивный дизайн** для работы на разных устройствах

## 🚀 Запуск

### Локально в браузере
1. Откройте файл `index.html` в любом современном браузере
2. Или запустите локальный сервер:
   ```bash
   python -m http.server 8000
   ```
   И откройте http://localhost:8000

### В Docker (для облака или изолированного запуска)
```bash
# Сборка образа
docker build -t mlm-game .

# Запуск (демо будет на http://localhost:8090)
docker run -p 8090:80 mlm-game
```

Или через Docker Compose:
```bash
docker compose up -d
# Демо: http://localhost:8080
```

### Публикация в облаке
- **Any cloud (AWS, GCP, Azure, DigitalOcean и т.д.)**: создайте VM, установите Docker, выполните `docker run -p 80:80 mlm-game` и откройте порт 80 в файрволе/security group.
- **Docker Hub**: `docker tag mlm-game yourusername/mlm-game && docker push yourusername/mlm-game` — затем на сервере: `docker run -p 80:80 yourusername/mlm-game`.
- **Cloud Run / App Engine / аналоги**: используйте образ из реестра и укажите порт 80.

## 📝 Как использовать на семинаре

### Вариант 1: Индивидуальная работа
1. Каждый студент открывает игру на своем устройстве
2. Заполняет пропуски в текстах
3. Проверяет свои ответы
4. Обсуждает результаты

### Вариант 2: Групповая работа
1. Покажите игру на проекторе
2. Студенты предлагают варианты заполнения пропусков
3. Обсуждайте, почему одни слова подходят лучше других
4. Сравните с правильными ответами

### Вариант 3: Соревнование
1. Разделите студентов на команды
2. Каждая команда заполняет тексты
3. Побеждает команда с наибольшим количеством правильных ответов

## 🎓 Педагогическая ценность

### Демонстрируемые концепции:
- **Контекстное понимание**: как слова вокруг помогают предсказать пропущенное слово
- **Синтаксис и семантика**: как грамматика и смысл влияют на выбор слова
- **Неоднозначность**: иногда подходит несколько слов
- **Обучение на примерах**: как модели учатся на больших текстовых корпусах

### Вопросы для обсуждения:
1. Какие слова было легче всего предсказать? Почему?
2. Были ли случаи, когда подходило несколько вариантов?
3. Как левый и правый контекст помогали в выборе слова?
4. Чем это отличается от предсказания следующего слова (как в GPT)?

## 🛠 Структура проекта

```
MLM game/
├── index.html      # Основной HTML-файл
├── style.css       # Стили и дизайн
├── script.js       # Логика игры
└── README.md       # Документация
```

## ✨ Возможности для расширения

Вы можете легко добавить новые тексты, отредактировав массив `texts` в файле `script.js`:

```javascript
{
    title: "Название текста",
    content: "Полный текст с теми словами, которые будут маскироваться",
    masks: [
        { word: "слово1", hint: "подсказка для слова1" },
        { word: "слово2", hint: "подсказка для слова2" }
    ]
}
```

### Идеи для дополнений:
- Добавить уровни сложности
- Интегрировать реальную языковую модель через API
- Добавить таймер для соревнований
- Сохранение результатов в локальное хранилище
- Экспорт результатов в CSV для анализа

## 📚 Связь с NLP

Эта игра демонстрирует базовый принцип MLM, который используется в:
- **BERT** (Bidirectional Encoder Representations from Transformers)
- **RoBERTa** (Robustly Optimized BERT Pretraining Approach)
- **ALBERT**, **DistilBERT** и других моделях

В реальных моделях:
- Маскируется ~15% токенов
- Модель обучается на миллионах текстов
- Используется двунаправленный контекст (слева и справа)
- После обучения модель fine-tuning для конкретных задач
