# Предсказание выживаемости на Титанике

Проект по машинному обучению для предсказания выживаемости пассажиров Титаника.

## 🎓 Две версии проекта

### Для семинара (студенты):
📖 **Начните с [README_SEMINAR.md](README_SEMINAR.md)**
- `titanic_seminar.py` - упрощённый скрипт для обучения
- Легко изменяемые параметры без программирования
- Задания на вычисление метрик вручную
- Пошаговые инструкции

### Продвинутая версия:
- `train_titanic.py` - полный скрипт с автоматическими метриками
- Подробная визуализация и анализ
- Для самостоятельного изучения

## Структура проекта

```
.
├── data/
│   └── train.csv                  # Датасет с данными о пассажирах
├── train_titanic.py               # Скрипт обучения модели
├── requirements.txt               # Зависимости проекта
├── roc_curve.png                  # ROC-кривая (автоматически создается)
├── precision_recall_curve.png     # Precision-Recall кривая (автоматически создается)
├── combined_curves.png            # Комбинированный график (автоматически создается)
└── README.md                      # Этот файл
```

## Установка

1. Установите зависимости:
```bash
pip install -r requirements.txt
```

## Запуск

Запустите скрипт обучения:
```bash
python train_titanic.py
```

## Описание данных

Датасет содержит информацию о пассажирах Титаника:

- **PassengerId**: Уникальный идентификатор пассажира
- **Survived**: Выжил ли пассажир (0 = Нет, 1 = Да) - целевая переменная
- **Pclass**: Класс билета (1 = 1-й класс, 2 = 2-й класс, 3 = 3-й класс)
- **Name**: Имя пассажира
- **Sex**: Пол (male/female)
- **Age**: Возраст в годах
- **SibSp**: Количество братьев/сестер/супругов на борту
- **Parch**: Количество родителей/детей на борту
- **Ticket**: Номер билета
- **Fare**: Стоимость билета
- **Cabin**: Номер каюты
- **Embarked**: Порт посадки (C = Cherbourg, Q = Queenstown, S = Southampton)

## Методология

1. **Предобработка данных:**
   - Заполнение пропусков в возрасте медианой
   - Заполнение пропусков в стоимости билета медианой
   - Заполнение пропусков в порте посадки модой
   - Кодирование категориальных признаков (пол, порт посадки)

2. **Модель:**
   - Random Forest Classifier
   - 100 деревьев
   - Максимальная глубина: 10

3. **Разделение данных:**
   - 80% - обучающая выборка
   - 20% - тестовая выборка (отложенная)

## Результаты

### Качество на тестовой выборке:
- **Accuracy**: 79.89%
- **ROC-AUC**: 83.70%

### Важность признаков:
1. Sex (пол) - 35.0%
2. Fare (стоимость билета) - 21.3%
3. Age (возраст) - 19.1%
4. Pclass (класс билета) - 12.5%

### Визуализация:

После запуска скрипт автоматически создает три графика:

1. **ROC Curve** (`roc_curve.png`)
   - Показывает соотношение True Positive Rate и False Positive Rate
   - ROC-AUC = 0.827 на тестовой выборке

2. **Precision-Recall Curve** (`precision_recall_curve.png`)
   - Показывает баланс между точностью и полнотой
   - Average Precision = 0.807 на тестовой выборке
   - PR-AUC = 0.806 на тестовой выборке

3. **Combined Curves** (`combined_curves.png`)
   - Комбинированный график с обеими кривыми
   - Сравнение train/test для анализа переобучения

### Выводы:
- Пол пассажира - самый важный признак для предсказания выживания
- Стоимость билета и класс также играют важную роль
- Модель показывает хорошее качество на отложенной выборке
- Умеренное переобучение (11% разница между train и test)
